apiVersion: v2
name: monitoring
description: Prometheus + Grafana monitoring stack
type: application
version: 1.0.8
appVersion: "1.0.8"

dependencies:
  - name: kube-prometheus-stack
    version: "87.4.0"   # Pin to latest stable; check https://github.com/prometheus-community/helm-charts
    repository: https://prometheus-community.github.io/helm-charts
apiVersion: v2
name: monitoring
description: Prometheus + Grafana monitoring stack
type: application
version: 1.0.2
appVersion: "1.0.2"

dependencies:
  - name: kube-prometheus-stack
    version: "84.5.0"   # Pin to latest stable; check https://github.com/prometheus-community/helm-charts
    repository: https://prometheus-community.github.io/helm-charts